<h1>Listing opinions</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Artist</th>
      <th>Like</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @opinions.each do |opinion| %>
      <tr>
        <td><%= opinion.user %></td>
        <td><%= opinion.artist %></td>
        <td><%= opinion.like %></td>
        <td><%= link_to 'Show', new_artist_opinion_path %></td>
        <td><%= link_to 'Edit', edit_artist_opinion_path %></td>
        <td><%= link_to 'Destroy', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Opinion', new_artist_opinion_path %>
